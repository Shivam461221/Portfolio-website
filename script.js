const typed = new Typed('.multiple-text', {
    strings: ["Frontend Developer", "Backend Developer", "Researcher", "Social Worker"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});